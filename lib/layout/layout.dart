/*import 'package:flutter/material.dart';
class TabBar extends StatelessWidget {
   TabBar({Key? key, required this.text, required this.image}) : super(key: key);
  String? text;
  String? image;
  @override
  Widget build(BuildContext context) {
    return DefaultTabController(
      length: 4,
      child: Scaffold(
        appBar: AppBar(
          bottom: TabBar(text: '', image: '') ,
        ),
      ),
    );
  }
}
*/